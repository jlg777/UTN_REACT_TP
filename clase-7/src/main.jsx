import React from 'react';
import ReactDOM from 'react-dom';
import ProductDetail from './producto.jsx';


ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <ProductDetail />
  </React.StrictMode>,
);